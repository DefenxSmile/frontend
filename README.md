# TableBook 🍽️

Современная система управления ресторанами и заведениями для создания планов залов и бронирования столов. Конкурентное решение для Restoplace с расширенным функционалом и улучшенным UX.

![Version](https://img.shields.io/badge/version-0.0.0-blue)
![React](https://img.shields.io/badge/React-19.2.0-61dafb)
![TypeScript](https://img.shields.io/badge/TypeScript-5.9.3-3178c6)
![License](https://img.shields.io/badge/license-MIT-green)

## 📋 Содержание

- [О проекте](#о-проекте)
- [Основные возможности](#основные-возможности)
- [Технологический стек](#технологический-стек)
- [Установка и запуск](#установка-и-запуск)
- [Структура проекта](#структура-проекта)
- [Руководство по использованию](#руководство-по-использованию)
- [Архитектура и типы данных](#архитектура-и-типы-данных)
- [API и интеграция](#api-и-интеграция)
- [Разработка](#разработка)
- [Планы развития](#планы-развития)

## 🎯 О проекте

**TableBook** — это полнофункциональная веб-платформа для управления ресторанами и заведениями, которая позволяет:

- **Администраторам** создавать и редактировать интерактивные планы залов с помощью мощного визуального редактора
- **Гостям** просматривать планы залов, выбирать свободные столы и бронировать их на удобное время

Проект разработан с использованием современных технологий и лучших практик UI/UX для обеспечения максимального удобства использования как на десктопе, так и на мобильных устройствах.

## ✨ Основные возможности

### Для администраторов

- 🎨 **Визуальный редактор планов залов**
  - Интерактивный canvas-редактор на базе Konva.js
  - Создание столов различных форм (круглые, прямоугольные, овальные, квадратные)
  - Рисование стен, дверей и окон
  - Множественные этажи с переключением между ними
  - Группировка и разгруппировка элементов
  - Масштабирование, панорамирование и сетка для точного позиционирования
  - Snap-to-grid для выравнивания элементов
  - Контекстное меню для быстрого доступа к действиям
  - Панель свойств в виде Drawer для редактирования параметров элементов

- 📊 **Управление клиентами**
  - Таблица всех заведений с возможностью добавления, редактирования и удаления
  - Быстрый доступ к конструктору и просмотру планов
  - Темная и светлая темы интерфейса

- 💾 **Экспорт и сохранение**
  - Генерация JSON структуры для сохранения на бекенде
  - Предпросмотр данных перед сохранением
  - Локальное хранение данных (localStorage) для разработки

### Для гостей

- 🗺️ **Просмотр планов залов**
  - Оптимизированный SVG-рендеринг для быстрой загрузки
  - Поддержка мобильных устройств с touch-жестами
  - Zoom и pan для удобной навигации
  - Визуализация статуса столов (свободен, занят, выбран)

- ⏰ **Система бронирования**
  - Интерактивный выбор времени через временную шкалу
  - Гибкий выбор произвольных временных интервалов
  - Визуализация доступности столов в реальном времени
  - Форма бронирования с валидацией
  - Выбор даты и количества гостей

- 📱 **Мобильная оптимизация**
  - Адаптивный дизайн для всех размеров экранов
  - Touch-жесты для zoom и pan
  - Оптимизированная производительность на мобильных устройствах

## 🛠 Технологический стек

### Основные технологии

- **React 19** — современная UI библиотека с последними возможностями
- **TypeScript** — строгая типизация для надежности кода
- **Vite** — быстрый сборщик и dev-сервер
- **React Router 7** — маршрутизация в приложении

### UI и стилизация

- **Material-UI (MUI) 7** — компонентная библиотека для быстрой разработки UI
- **Tailwind CSS 4** — utility-first CSS фреймворк
- **SCSS** — препроцессор CSS для модульных стилей
- **Emotion** — CSS-in-JS решение от MUI

### Canvas и графика

- **Konva.js 10** — мощная 2D canvas библиотека
- **React-Konva 19** — официальная React интеграция для Konva

### Управление состоянием

- **React Query (@tanstack/react-query)** — управление серверным состоянием и кэшированием
- **React Context API** — управление темой приложения

### Инструменты разработки

- **ESLint** — линтинг кода
- **TypeScript ESLint** — проверка типов
- **PostCSS** — обработка CSS
- **Autoprefixer** — автоматические префиксы для браузеров

## 🚀 Установка и запуск

### Требования

- Node.js >= 18.0.0
- npm >= 9.0.0

### Установка зависимостей

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:5173`

### Сборка для production

```bash
npm run build
```

Собранные файлы будут находиться в папке `dist/`

### Предпросмотр production сборки

```bash
npm run preview
```

### Линтинг кода

```bash
npm run lint
```

## 📁 Структура проекта

```
restoreserve/
├── public/                 # Статические файлы
├── src/
│   ├── api/               # API клиенты и запросы
│   │   └── floorPlanApi.ts
│   ├── components/         # Переиспользуемые компоненты
│   │   ├── BookingPanel/   # Панель бронирования
│   │   ├── FloorPlanEditor/ # Редактор планов залов
│   │   │   ├── FloorPlanEditor.tsx
│   │   │   ├── PropertiesPanel.tsx
│   │   │   └── TableShapeSelector.tsx
│   │   ├── FloorPlanViewer/ # Просмотр планов (Konva)
│   │   ├── FloorPlanViewerSVG/ # Просмотр планов (SVG для гостей)
│   │   ├── Layout/         # Компонент макета с навигацией
│   │   ├── TimeLine/       # Компонент временной линии
│   │   └── TimeRangeSelector/ # Селектор временного диапазона
│   ├── contexts/           # React Context провайдеры
│   │   └── ThemeContext.tsx
│   ├── hooks/              # Кастомные React хуки
│   │   └── useFloorPlan.ts
│   ├── pages/              # Страницы приложения
│   │   ├── AdminPage/      # Административная панель
│   │   │   ├── AdminClientsPage.tsx
│   │   │   ├── AdminEditorPage.tsx
│   │   │   └── AdminViewerPage.tsx
│   │   ├── HomePage/       # Главная страница
│   │   └── UserPage/       # Страница для гостей
│   ├── types/              # TypeScript типы и интерфейсы
│   │   ├── booking.ts
│   │   ├── client.ts
│   │   └── floorPlan.ts
│   ├── utils/              # Утилиты и хелперы
│   │   ├── storage.ts      # Работа с localStorage
│   │   └── timeSlots.ts    # Генерация временных слотов
│   ├── App.tsx             # Главный компонент приложения
│   ├── main.tsx            # Точка входа
│   └── index.css           # Глобальные стили
├── package.json
├── tsconfig.json           # Конфигурация TypeScript
├── vite.config.ts          # Конфигурация Vite
├── tailwind.config.js       # Конфигурация Tailwind
└── README.md               # Документация
```

## 📖 Руководство по использованию

### Для администраторов

#### 1. Управление клиентами

Перейдите на страницу `/admin` для просмотра списка всех заведений.

- **Добавление нового клиента**: Нажмите кнопку "Добавить клиента" и заполните форму
- **Редактирование плана**: Нажмите на три точки в строке клиента → "Редактировать план"
- **Просмотр плана**: Нажмите на три точки → "Просмотреть план"
- **Удаление клиента**: Нажмите на три точки → "Удалить"

#### 2. Редактор планов залов

##### Основные инструменты

- **Выбор (Select)** — выбор и перемещение элементов
- **Стол** — добавление столов различных форм
- **Стена** — рисование стен (drag-to-draw)
- **Дверь** — добавление дверей
- **Окно** — добавление окон

##### Управление элементами

- **Перемещение**: Выберите элемент и перетащите его
- **Изменение размера**: Выберите элемент и потяните за углы трансформера
- **Поворот**: Выберите элемент и потяните за маркер поворота
- **Группировка**: Выделите несколько элементов (Ctrl/Cmd + клик или выделение области) и нажмите "Группировать"
- **Разгруппировка**: Выберите группу и нажмите "Разгруппировать"
- **Дублирование**: Правый клик на элементе → "Дублировать"
- **Удаление**: Правый клик на элементе → "Удалить" или клавиша Delete

##### Управление этажами

- **Добавление этажа**: Нажмите кнопку "+" в панели этажей
- **Переключение между этажами**: Кликните на название этажа
- **Редактирование названия**: Двойной клик на названии этажа или клик на иконку редактирования
- **Удаление этажа**: Нажмите на иконку удаления (требуется подтверждение)

##### Навигация по canvas

- **Zoom**: Используйте колесико мыши или кнопки +/- в левом верхнем углу
- **Pan**: Зажмите Spacebar и перетащите мышь, или используйте среднюю кнопку мыши
- **Сетка**: Переключите отображение сетки кнопкой в панели управления
- **Fit to screen**: Нажмите кнопку для автоматического масштабирования под размер экрана

##### Сохранение плана

1. Нажмите кнопку "Сохранить план" в правом верхнем углу canvas
2. Заполните форму:
   - Имя клиента
   - Название заведения
   - Настройки работы заведения (опционально)
3. Просмотрите сгенерированный JSON в текстовом поле
4. Нажмите "Сохранить" для отправки на бекенд

### Для гостей

#### 1. Загрузка плана зала

Перейдите на страницу `/user` и:

- **Вариант 1**: Вставьте JSON структуру плана в текстовое поле и нажмите "Загрузить схему"
- **Вариант 2**: Загрузите JSON файл через кнопку "Загрузить из файла"

#### 2. Выбор стола и времени

1. **Выберите стол**: Кликните на стол на схеме зала
2. **Выберите дату**: Используйте календарь в компоненте выбора времени
3. **Выберите время**: 
   - Кликните на временной шкале для установки начала и конца бронирования
   - Или перетащите маркеры для точной настройки времени
4. **Заполните форму бронирования**:
   - Количество гостей
   - Имя гостя
   - Телефон
   - Email (опционально)
5. Нажмите "Забронировать"

#### 3. Навигация по плану

- **Zoom**: Используйте жесты pinch-to-zoom на мобильных или колесико мыши на десктопе
- **Pan**: Перетащите план пальцем или мышью
- **Переключение этажей**: Используйте выпадающий список этажей

## 🏗 Архитектура и типы данных

### Основные типы

#### FloorPlanElement

```typescript
interface FloorPlanElement {
  id: string
  type: 'table' | 'wall' | 'door' | 'window' | 'group'
  x: number
  y: number
  width?: number
  height?: number
  radius?: number
  rotation?: number
  fill?: string
  stroke?: string
  strokeWidth?: number
  label?: string
  tableShape?: 'circle' | 'rectangle' | 'oval' | 'square'
  capacity?: number
  isAvailable?: boolean
  zIndex?: number
  children?: string[] // Для групп
  isGroup?: boolean
}
```

#### FloorPlanData

```typescript
interface FloorPlanData {
  stage: {
    width: number
    height: number
    scale?: number
    offsetX?: number
    offsetY?: number
  }
  floors: Floor[]
  currentFloorId: string
  metadata?: {
    clientId?: string
    clientName?: string
    venueId?: string
    venueName?: string
    createdAt?: string
    updatedAt?: string
  }
  venueSettings?: VenueSettings
}
```

#### Booking

```typescript
interface Booking {
  id: string
  tableId: string
  floorId: string
  timeSlot: TimeSlot
  guestName: string
  guestPhone: string
  guestEmail?: string
  numberOfGuests: number
  status: 'pending' | 'confirmed' | 'cancelled' | 'completed'
  createdAt: string
  updatedAt: string
  notes?: string
}
```

Полное описание всех типов находится в папке `src/types/`.

### Структура JSON для сохранения

Пример структуры JSON, которая генерируется при сохранении плана:

```json
{
  "stage": {
    "width": 2000,
    "height": 1500,
    "scale": 1,
    "offsetX": 0,
    "offsetY": 0
  },
  "floors": [
    {
      "id": "floor-1",
      "name": "Первый этаж",
      "level": 1,
      "elements": [
        {
          "id": "table-1",
          "type": "table",
          "x": 500,
          "y": 400,
          "radius": 50,
          "tableShape": "circle",
          "capacity": 4,
          "isAvailable": true,
          "label": "Стол 1"
        }
      ]
    }
  ],
  "currentFloorId": "floor-1",
  "metadata": {
    "clientName": "Ресторан 'Уютный'",
    "venueName": "Главный зал"
  },
  "venueSettings": {
    "operatingHours": {
      "monday": { "open": "10:00", "close": "22:00" },
      "tuesday": { "open": "10:00", "close": "22:00" }
    },
    "defaultBookingDuration": 120,
    "advanceBookingDays": 30,
    "timeSlotInterval": 15
  }
}
```

## 🔌 API и интеграция

### Текущая реализация

На данный момент данные хранятся в `localStorage` для разработки. Для production необходимо интегрировать с backend API.

### Планируемые endpoints

#### Управление клиентами

- `GET /api/clients` — получить список клиентов
- `POST /api/clients` — создать нового клиента
- `GET /api/clients/:id` — получить клиента по ID
- `PUT /api/clients/:id` — обновить клиента
- `DELETE /api/clients/:id` — удалить клиента

#### Управление планами залов

- `GET /api/clients/:clientId/floor-plans` — получить планы зала клиента
- `POST /api/clients/:clientId/floor-plans` — создать план зала
- `GET /api/floor-plans/:id` — получить план зала по ID
- `PUT /api/floor-plans/:id` — обновить план зала
- `DELETE /api/floor-plans/:id` — удалить план зала

#### Бронирования

- `GET /api/bookings` — получить список бронирований
- `POST /api/bookings` — создать бронирование
- `GET /api/bookings/:id` — получить бронирование по ID
- `PUT /api/bookings/:id` — обновить бронирование
- `DELETE /api/bookings/:id` — отменить бронирование
- `GET /api/floor-plans/:id/availability` — получить доступность столов

### Интеграция с React Query

Все API запросы должны использовать React Query для кэширования и управления состоянием:

```typescript
import { useQuery, useMutation } from '@tanstack/react-query'

const { data, isLoading } = useQuery({
  queryKey: ['clients'],
  queryFn: () => fetchClients()
})
```

## 💻 Разработка

### Стиль кода

- Используйте TypeScript для всех новых файлов
- Следуйте правилам ESLint
- Используйте функциональные компоненты и хуки
- Применяйте принципы SOLID и DRY

### Коммиты

Используйте осмысленные сообщения коммитов:

```
feat: добавлена возможность группировки элементов
fix: исправлена ошибка при сохранении плана
refactor: оптимизирован рендеринг canvas
docs: обновлена документация
```

### Тестирование

Планируется добавить:
- Unit тесты (Jest + React Testing Library)
- E2E тесты (Playwright или Cypress)
- Визуальное тестирование (Chromatic)

### Производительность

- Используйте `React.memo` для оптимизации рендеринга
- Применяйте `useMemo` и `useCallback` где необходимо
- Оптимизируйте работу с Konva через правильное использование `Layer` и `Group`

## 🗺 Планы развития

### Ближайшие задачи

- [ ] Интеграция с backend API
- [ ] Система аутентификации и авторизации
- [ ] Уведомления о бронированиях (email, SMS)
- [ ] Календарь бронирований для администраторов
- [ ] Экспорт планов в PNG/PDF
- [ ] Дополнительные элементы планировки (барная стойка, кухня, туалеты)
- [ ] История изменений планов
- [ ] Шаблоны планов залов

### Долгосрочные цели

- [ ] Мобильное приложение (React Native)
- [ ] Интеграция с POS-системами (R-Keeper и др.)
- [ ] Аналитика и отчеты
- [ ] Мультиязычность
- [ ] Интеграция с системами онлайн-оплаты
- [ ] Система лояльности для гостей

## 📝 Лицензия

MIT License

## 👥 Контакты

Для вопросов и предложений создавайте Issues в репозитории проекта.

---

**TableBook** — Современное решение для управления ресторанами и заведениями 🍽️✨
